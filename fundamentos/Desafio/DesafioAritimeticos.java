package fundamentos.Desafio;

public class DesafioAritimeticos {
	public static void main(String[] args) {
		int a = (1-5) * (2-7)/2;
			int aa = (int)Math.pow(a, 2);
		int b = (6*(3+2));
			int bb = (int)Math.pow(b , 2);
				bb = bb / (3 *2); 			
					int ab = bb - aa;
					int aabb = (int)Math.pow(ab, 3);
						int divisor = 10;
							int divisorp = (int)Math.pow(divisor, 3);		
							System.out.print("o resultado enfim é : " + aabb / divisorp);
		 					
						
						
			
		
		
		
		
		
		
		
		
	
	}
}
